name: Check Typos

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check-typos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check for typos
        uses: crate-ci/typos@v1
